# Saves de sessão

Os exports do painel ("Exportar JSON") caem em **Downloads**.
Mova o arquivo pra cá mantendo o nome padrão `rpg_ds_YYYY-MM-DD.json`.
Pra restaurar, use o botão **Carregar JSON** no painel do mestre.
